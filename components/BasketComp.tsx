import React, { useState } from 'react';

interface CartItem {
  id: number;
  name: string;
  price: number;
  quantity: number;
}

const BasketComp = () => {

    return (
        <div className='divwrapper'>
          {/* kolla single product, ändra beteende när jag har i korgen */}
        </div>
    )
}

export default BasketComp